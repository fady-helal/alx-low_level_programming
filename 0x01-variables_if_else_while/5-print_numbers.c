#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: print all single digit
 *
 * Return: (0)success
*/
int main(void)
{
for (int i = 0 ; i <= 9 ; i++)
{
pritnf("%d", i);
}
printf("\n");
return (0);
}
