#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: print Hex decimal
 *
 * Return: (0)success
*/
int main(void)
{
int x, y;
for (x = 48 ; x < 58 ; x++)
{
	putchar(x);
}
for (y = 65 ; y < 71 ; y++)
{
	putchar(y);
}
	putchar('\n');
return (0);
}

